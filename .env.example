# =====================================================================
# KISSARIYA — VARIABLES D'ENVIRONNEMENT (EXEMPLE — AUCUN SECRET RÉEL)
# Copier ce fichier vers `.env.local` (développement) et remplir.
# ⚠️ Ne JAMAIS committer `.env*` contenant de vraies clés.
# =====================================================================

# ─────────────────────────────────────────────────────────────
# APPLICATION (utilisé par src/integrations/supabase/client.ts)
# ─────────────────────────────────────────────────────────────
# URL du projet Supabase (Dashboard → Settings → API).
VITE_SUPABASE_URL="https://ygkeuhatokvkdwwoccty.supabase.co"

# Clé PUBLIQUE "anon" du projet (publiable côté navigateur,
# protégée par RLS côté base de données).
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inlna2V1aGF0b2t2a2R3d29jY3R5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODUyNzAyNzksImV4cCI6MjEwMDg0NjI3OX0.E2xOlYdNO_rh7yMN6h3GcygMFQ2d0claAq7jzaH3WUg"

# ─────────────────────────────────────────────────────────────
# SENTRY (optionnel — monitoring des erreurs frontend)
# ─────────────────────────────────────────────────────────────
VITE_SENTRY_DSN="https://6769946aaba8d3325e22de4e155ea422@o4511841426800640.ingest.de.sentry.io/4511841433747536"
VITE_SENTRY_ENVIRONMENT="production"
# ─────────────────────────────────────────────────────────────
# TEST DE CHARGE k6 (scripts/k6-load-test.js)
# Ces variables ne sont PAS lues par l'application — seulement
# par la commande k6 (sans aucun secret service_role).
# ─────────────────────────────────────────────────────────────
# URL du projet Supabase DÉDIÉ AUX TESTS (jamais la production).
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Clé "anon" du projet de test (publique, sûre à exposer).
SUPABASE_ANON_KEY=eyJhbGciOi...

# Compte authentifié NON-ADMIN créé dans le projet de test,
# utilisé par les scénarios auth + commandes de test.
SUPABASE_TEST_EMAIL=hassanain1000@gmail.com
SUPABASE_TEST_PASSWORD=mohamed6278

# Charge du test k6 (free plan : rester ≤ 40).
MAX_VUS=20
DURATION=2m

# ─────────────────────────────────────────────────────────────
# SUPABASE DASHBOARD — configuration requise une seule fois
# (voir SCALABILITY_SECURITY_VERIFICATION.md pour le détail)
#   • Authentication → Providers → Email : laisser l'option "Allow new
#     users to sign up" DÉSACTIVÉE — il n'y a AUCUNE inscription publique.
#   • Créer les comptes admin manuellement : Authentication → Users →
#     Create user (email + mot de passe). Chaque compte ainsi créé est
#     un compte admin (tout compte Supabase Auth = admin).
#   • SQL Editor : exécuter supabase/database.sql (idempotent).
#     AUCUNE table admin_users ni colonne role n'est requise.
# ─────────────────────────────────────────────────────────────
