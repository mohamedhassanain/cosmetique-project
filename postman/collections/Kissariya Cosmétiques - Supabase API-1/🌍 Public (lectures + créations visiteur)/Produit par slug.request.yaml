$kind: http-request
description: Équivalent de fetchProductBySlug(). Modifiez la variable 'slug' dans l'environnement.
url: "{{supabase_url}}/rest/v1/products?select=id,name,slug,description,ingredients,how_to_use,price,original_price,is_promotion,is_active,image_url,video_url,category_id,subcategory_id,stock_quantity,weight_grams,brand,location_city,location_url,show_location,created_at,categories(name,slug),subcategories(name),product_images(id,url,sort_order)&slug=eq.{{slug}}&is_active=eq.true"
method: GET
headers:
  apikey: "{{anon_key}}"
queryParams:
  select: id,name,slug,description,ingredients,how_to_use,price,original_price,is_promotion,is_active,image_url,video_url,category_id,subcategory_id,stock_quantity,weight_grams,brand,location_city,location_url,show_location,created_at,categories(name,slug),subcategories(name),product_images(id,url,sort_order)
  slug: eq.{{slug}}
  is_active: eq.true
order: 4000
