$kind: http-request
description: Équivalent de fetchActivePromos().
url: "{{supabase_url}}/rest/v1/promos?select=*&is_active=eq.true&order=sort_order.asc"
method: GET
headers:
  apikey: "{{anon_key}}"
queryParams:
  select: "*"
  is_active: eq.true
  order: sort_order.asc
order: 7000
