$kind: http-request
url: "{{supabase_url}}/rest/v1/promos?id=eq.{{promo_id}}"
method: DELETE
headers:
  apikey: "{{anon_key}}"
  Authorization: Bearer {{access_token}}
queryParams:
  id: eq.{{promo_id}}
order: 4000
