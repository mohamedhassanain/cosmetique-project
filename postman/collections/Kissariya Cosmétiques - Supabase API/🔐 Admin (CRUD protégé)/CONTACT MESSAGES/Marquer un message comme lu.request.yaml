$kind: http-request
url: "{{supabase_url}}/rest/v1/contact_messages?is_read=eq.false"
method: PATCH
headers:
  apikey: "{{anon_key}}"
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
queryParams:
  is_read: eq.false
body:
  type: json
  content: |-
    {
      "is_read": true
    }
order: 2000
